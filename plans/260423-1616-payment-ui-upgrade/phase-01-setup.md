# Phase 01: Setup & Dependencies
Status: ✅ Completed

## Objective
Chuẩn bị môi trường và cài đặt các thư viện cần thiết để triển khai các hiệu ứng cao cấp.

## Implementation Steps
1. [x] Kiểm tra lại cấu hình MUI v9 trong dự án.
2. [x] Cài đặt `framer-motion` để xử lý hiệu ứng nhảy số (counting up) và các micro-animations.
3. [x] Định nghĩa các hằng số CSS cho Glassmorphism (blur, border color, opacity).

## Files to Create/Modify
- `package.json` - Cập nhật dependencies.
- `src/theme/index.ts` (nếu có) - Cập nhật theme constants.

## Test Criteria
- [x] Thư viện `framer-motion` được cài đặt thành công.
- [x] Dự án vẫn chạy bình thường sau khi install.
