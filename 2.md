# Báo cáo Điều tra & Sửa lỗi Responsive

## 1. Hiểu lại vấn đề của Anh
Qua mô tả chi tiết của anh: *"Có thể vuốt ngang để xem được, nhưng lúc mới vào thì phải tự dùng tay zoom out (pinch-to-zoom) để thấy toàn bộ web"*.
- **Điều này có nghĩa là:** Điện thoại của anh không tự động zoom out, nhưng do nội dung trang web (thực chất là bị cái bảng đẩy ra) rộng hơn màn hình điện thoại (ví dụ màn hình 390px, nhưng trang web bị giãn ra 800px).
- **Hệ quả:** Lúc mới tải trang, trình duyệt chỉ hiển thị 390px góc bên trái. Anh phải lấy tay vuốt ngang (hoặc tự lấy 2 ngón tay thu nhỏ lại - manual zoom out) thì mới nhìn thấy hết giao diện.
- **Yêu cầu của anh:** Muốn giao diện web vừa khít màn hình điện thoại ngay từ đầu (không cần manual zoom out, không bị vuốt ngang thân trang), và **chỉ phần bảng dữ liệu mới có thể vuốt ngang**.

> 💡 **Tin vui cho anh:** Lỗi này ở trang Bệnh nhân hay trang Kho thuốc **đều chung một nguyên nhân gốc**! Em đã sửa tận gốc để fix cho **CẢ 2 TRANG** chứ không bỏ qua trang Kho thuốc đâu ạ. Anh yên tâm nhé!

## 2. Nguyên nhân kỹ thuật (Tại sao web bị phình to?)
Nguyên nhân cốt lõi nằm ở cách CSS Flexbox hoạt động trong file giao diện tổng (`DashboardShell.tsx`).

1. Ứng dụng dùng cấu trúc: `<div className="flex">` -> `<main className="flex-1">` -> `{children}`.
2. Bên trong các trang (Bệnh nhân, Kho thuốc) có chứa thẻ `<table>`. Các thẻ table này có class `whitespace-nowrap` (không xuống dòng), nên chúng ép chiều rộng phải đủ to (ví dụ 800px) mới chứa hết chữ.
3. Trong Flexbox, thẻ `<main className="flex-1">` có một thuộc tính ngầm định là `min-width: auto`. Tức là: **Nó sẽ không bao giờ chịu co lại nhỏ hơn nội dung bên trong nó**.
4. Vì thế, thẻ `<main>` bị cái bảng ép phải rộng 800px. Khi `<main>` rộng 800px, toàn bộ trang web (body) bị giãn ra 800px. Thế là anh thấy web tràn màn hình và phải tự zoom out!

## 3. Em đã sửa như thế nào?
Em đã can thiệp trực tiếp vào file `/src/components/features/DashboardShell.tsx`.

Chỉ với việc thêm class **`min-w-0`** vào thẻ `<main>`:
```tsx
// Trước đây:
<main className="flex-1 px-4 pb-4 pt-24 md:px-6 md:pb-6 md:pt-24 md:ml-60 mb-16 md:mb-0 transition-all duration-300">

// Đã sửa thành:
<main className="flex-1 min-w-0 px-4 pb-4 pt-24 md:px-6 md:pb-6 md:pt-24 md:ml-60 mb-16 md:mb-0 transition-all duration-300">
```

### Tại sao class `min-w-0` lại vi diệu như vậy?
- Thêm `min-w-0` (min-width: 0) là cách ép Flexbox phá vỡ quy tắc `min-width: auto`.
- Lúc này, thẻ `<main>` được phép thu nhỏ lại vừa vặn với kích thước màn hình điện thoại (390px) mà không bị phụ thuộc vào nội dung bên trong.
- Khi `<main>` đã co lại 390px, trang web sẽ vừa khít màn hình điện thoại 100%. Anh **không cần manual zoom out** nữa!
- Cùng lúc đó, các thẻ chứa bảng `<div className="overflow-x-auto">` sẽ nhận ra thẻ cha của mình chỉ rộng 390px, nên chúng sẽ **kích hoạt thanh cuộn ngang độc lập** cho riêng cái bảng.

=> **Kết quả:** Giao diện tổng vừa khít, thanh navbar/sidebar vừa khít. Chỉ có khu vực bảng là anh lấy tay vuốt ngang được. Khắc phục trọn vẹn cả trang Bệnh nhân lẫn trang Kho thuốc!
